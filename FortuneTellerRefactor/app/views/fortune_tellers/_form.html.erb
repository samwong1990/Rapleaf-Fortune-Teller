<%= form_for(@fortune_teller) do |f| %>
  <% if @fortune_teller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fortune_teller.errors.count, "error") %> prohibited this fortune_teller from being saved:</h2>

      <ul>
      <% @fortune_teller.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :dataset %><br />
    <%= f.text_area :dataset , :rows => 6%>
  </div>
  <div class="actions">
    <%= f.submit 'Guide me Sam!', :class => 'btn primary'%>
  </div>
<% end %>
