<%= form_for(@query) do |f| %>
  <% if @query.errors.any? %>
    <div id="error_explanation">
      <h2>There are <%= pluralize(@query.errors.count, "error") %> things I want to know from you, don't be shy.</h2>

      <ul>
      <% @query.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>

    </div>
  <% end %>

  <div class="field">
    <%= f.label :'My Name is' %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :'My email is' %><br />
    <%= f.text_field :email %>
  </div>

	<p>What are your best friends' email?</p>
  <div class="field">
    <%= f.label :'These are my friends\' email' %><br />
    <%= f.text_area :dataset ,rows: 5 %>
  </div>
  <div class="actions">
    <%= f.submit :'Guide me Sam, what\'s in me?'%>
  </div>
<% end %>
